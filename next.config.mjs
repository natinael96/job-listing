/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['via.placeholder.com', 'avatar.iran.liara.run', 'res.cloudinary.com'],
    },
  };
  
  export default nextConfig;